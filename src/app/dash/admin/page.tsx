import Form from "@/components/admin/form";
import { NextPage } from "next";

interface Props {}

const Page: NextPage<Props> = ({}) => {
  return (
    <div className="">
      <Form />{" "}
    </div>
  );
};

export default Page;
